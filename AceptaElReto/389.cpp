#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

void resolver() {

}

bool resuelveCaso() {
	string operando1, operando2, operador, resultado, igual;
	cin >> operando1 >> operador >> operando2 >> igual >> resultado;
	if (!cin) return false;
	vector <int> charToInt(30);

}

int main() {
	while (resuelveCaso());
	return 0;
}